set(PACKAGE_VERSION @FALCON_VERSION_ID@)

set(PACKAGE_VERSION_COMPATIBLE false)
if(PACKAGE_FIND_VERSION VERSION_LESS @FALCON_VERSION_ID@)
  set(PACKAGE_VERSION_COMPATIBLE true)
endif()
if(PACKAGE_FIND_VERSION VERSION_EQUAL @FALCON_VERSION_ID@)
  set(PACKAGE_VERSION_EXACT true)
endif()
