load inet

addr = Address("www.falconpl.org:http")
addr.resolve()
addr.addresses.foreach(printl)

x = Socket()
str = m""
x.connect(addr)
x.send("GET / HTTP/1.0\r\nHost: www.falconpl.org\r\n\r\n")
while x.recv(str, 10000)
   str.isText = true
   > str
end

x.close()