/*
998 series -- book samples

@name Wiki sample - Full syntactic macro

@output
10 + 10 + 100 = 120
100 + 10 * 100 = 1100
@endoutput
*/

function& sumMacro( p0, p1 )
   return& {[] ^~p0 + ^~p1 }
end

function& multMacro( p0, p1 )
   return& {[] ^~p0 * ^~p1 }
end

a = 10
b = 100

macro = sumMacro
> "10 + 10 + 100 = ", a + macro(a,b)   // 120
macro = multMacro
> "100 + 10 * 100 = ", b + macro(a,b)   // 1100
