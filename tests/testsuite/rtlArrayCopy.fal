/****************************************************************************
* Falcon test suite
*
*
* ID: 102e
* Category: rtl
* Subcategory: array
* Short: Array copy
* Description:
*   Copy an array. Functional equivalent of partitioning via range.
* [/Description]
*
****************************************************************************/

array = [ 1, 2, "alpha", 3,  "beta", 4 ]
arr1 = arrayCopy( array )
arr1[0] = "changed"
if array[0] != 1: failure( "Whole copy" )

arr1 = arrayCopy( array, 3 )
if len( arr1 ) != 3 or arr1[0] != 3: failure( "Copy with start" )

arr1 = arrayCopy( array, 1, 5 )
if len( arr1 ) != 4 or arr1[0] != 2: failure( "Copy with start and end" )

arr1 = arrayCopy( array, 4, 2 )
if len( arr1 ) != 3 or arr1[0] != "beta": failure( "Copy with reverse start and end" )

arr1 = arrayCopy( array, -3 )
if len( arr1 ) != 3 or arr1[2] != 4: failure( "Copy with negative start" )

arr1 = arrayCopy( array, 1, -1 )
if len( arr1 ) != 5 or arr1[4] != 4: failure( "Copy with start and negative end" )

arr1 = arrayCopy( array, -4, -1 )
if len( arr1 ) != 4 or arr1[0] != "alpha": failure( "Copy with negative start and negative end" )

arr1 = arrayCopy( array, -1, -5 )
if len( arr1 ) != 4 or arr1[0] != 4: failure( "Copy with reverse negative start and negative end" )

success()

/* End of file */
