/*
@name Array methods - push
@category Core-array

@output
Array methods - push
Test as a method:
[], "z": ["z"]
[], "b", "c", "d": ["b", "c", "d"]
["a"], "b", "c": ["a", "b", "c"]
Test as a static function:
[], "z": ["z"]
[], "b", "c", "d": ["b", "c", "d"]
["a"], "b", "c": ["a", "b", "c"]
Done
@endoutput
*/

> "Array methods - push"

tests = .[ 
   .[ .[] 'z' ]
   .[ .[] 'b' 'c' 'd' ]
   .[ .['a'] 'b' 'c' ]
]

function test( code, x )
   >> ", ".merge(^[x] {(v) ^+ v.describe() } =>[]), ": "
   code(x)
   > x[0].describe()
end

tests1 = [] 
for arr in tests
   arr1 = arr.clone()
   arr1[0] = arr[0].clone()
   tests1 += arr1
end

> "Test as a method:"
^[ tests ] .[test {(x) x.len > 1 ? call(x[0].push, x[1:]) : call(x[0].push) }]

> "Test as a static function:"
^[ tests1 ] .[test {(x) call( Array.push, x) }]

> "Done"
