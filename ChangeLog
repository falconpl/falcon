Dynlib (0.9.3)
   - Minor: Linked against 0.8.14
   - Major: Added function set to manipulate C structures:
            stringToPtr, memBufToPtr, memBufFromPtr, setStruct, getStruct
            and memSet.
   - Bugfix: limitBuffer without size was doing endless loop.
   
Dynlib (0.9.2)
   - Fixed: toString to display safe parameters formats.
   - Fixed: double floating point parameter and return values.
   - Added: function derefPtr() for double dereference.
   - Added: function dynExt function returning system specific
     dynlib extension.
   - Fixed: Empty parameter specificators may not block non-empty
            calls.

Dynlib (0.9)
   - This is 1.0 release candidate.

